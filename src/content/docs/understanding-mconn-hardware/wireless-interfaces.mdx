---
title: Wireless Interfaces
description: Overview of wireless interfaces available on the MConn
sidebar:
  order: 5
---

## WiFi

The MConn display uses a **Redpine RS9113** WiFi module and supports two operating modes:

- **AP Mode (Access Point)** — The display broadcasts its own WiFi hotspot. Other devices connect to it. This is the default mode.
- **Station Mode (Client)** — The display connects to an existing WiFi network as a client.

### AP Mode (Access Point / Hotspot)

In **AP Mode**, the MConn Display broadcasts its own WiFi hotspot. Other devices such as phones, tablets, or laptops can connect to it directly. This is the **default operating mode**.

#### Switching to AP Mode

##### Step 1 — Set the SSID and Password for the hotspot

The AP Mode credentials are stored in `/etc/wpa_supplicant.conf`. Since the root filesystem is read-only, remount it as writable before editing:

```bash
mount -o remount,rw /
nano /etc/wpa_supplicant.conf
```

Edit the `ssid` and `psk` fields in the `network` block:

```
network={
        ssid="Your-Hotspot-Name"
        frequency=2412
        pairwise=CCMP
        group=CCMP
        key_mgmt=WPA-PSK
        psk="Your-Hotspot-Password"
        proto=WPA2
        mode=2
        priority=2
}
```

Save and exit (`Ctrl+X`, then `Y`, then `Enter`).

:::caution
The password (`psk`) must be between 8 and 63 characters. Shorter passwords will be rejected.
:::

:::note
The `frequency` field sets the WiFi channel. Common 2.4 GHz options are `2412` (channel 1), `2437` (channel 6), and `2462` (channel 11).
:::

##### Step 2 — Switch to AP Mode and restart

```bash
onebox-set-mode ap && systemctl restart network-wireless.service
```

##### Step 3 — Verify the hotspot is active

```bash
wpa_cli -i wifi1 status
ifconfig wifi1
```

The `ifconfig` output should show the IP address `192.168.87.1`. The hotspot should now be visible on nearby devices. 

:::note
Connecting devices will receive an IP address in the `192.168.87.10` – `192.168.87.50` range.
:::

### Station (Client) Mode

In **Station (Client)** Mode, the MConn Display connects to an existing WiFi network. This is useful when the display needs access to a local network or the internet.

#### Switching to Station (Client) Mode

##### Step 1 — Set the credentials for the network to connect to

The Station (Client) Mode credentials are stored in `/etc/sta_settings.conf`. Since the root filesystem is read-only, remount it as writable before editing:

```bash
mount -o remount,rw /
nano /etc/sta_settings.conf
```

Edit the `ssid` and `psk` fields in the `network` block to match the network you want to connect to:

```
network={
        ssid="Your-Network-Name"
        pairwise=CCMP TKIP
        group=CCMP TKIP
        key_mgmt=WPA-PSK
        psk="Your-Network-Password"
        proto=WPA2 WPA
        priority=1
        scan_ssid=1
}
```

Save and exit (`Ctrl+X`, then `Y`, then `Enter`).

:::caution
The password (`psk`) must be between 8 and 63 characters. Shorter passwords will be rejected.
:::

:::tip
`scan_ssid=1` allows connecting to hidden networks. Set it to `0` for visible networks if desired.
:::

##### Step 2 — Switch to Station (Client) Mode and restart

```bash
onebox-set-mode sta && systemctl restart network-wireless.service
```

##### Step 3 — Verify the connection

```bash
wpa_cli -i wifi0 status
ifconfig wifi0
```

The `wpa_cli` output should show `wpa_state=COMPLETED` and `ifconfig` should show an IP address assigned by your network's router.

## Network Address Reference

| Interface | Mode    | IP Address           | Role                            |
|-----------|---------|----------------------|---------------------------------|
| `wifi1`   | AP      | `192.168.87.1`       | Display acts as hotspot         |
| `wifi0`   | Station | Assigned by router   | Display connects to a network   |
| `eth0`    | Wired   | `192.168.57.1`       | Wired Ethernet                  |
---

## Bluetooth

The MConn Display uses the **Redpine RS9113** wireless module for Bluetooth, which is the same module for WiFi. The RS9113 supports simultaneous WiFi and Bluetooth operation through its COEX (coexistence) mode. The Bluetooth stack on the display runs **BlueZ 5.50**, the standard Linux Bluetooth protocol stack, exposed through the `hci0` interface over SDIO.

The display supports both **Bluetooth Classic** and **Bluetooth Low Energy (BLE)**, and is managed via the standard `bluetoothctl` command-line tool.

:::note
The `bluetooth.service` is enabled and starts automatically at boot. However, the underlying `hci0` interface depends on the `onebox` driver stack being initialized first. If Bluetooth is not working after a reboot, follow the steps in the [Starting Bluetooth Manually](#starting-bluetooth-manually) section below.
:::

### Checking if Bluetooth is Running

To check whether the Bluetooth service is active:

```bash
systemctl status bluetooth
```

To check whether the `hci0` interface is up:

```bash
hciconfig -a
```

The output should show `UP RUNNING` and the device's Bluetooth MAC address:

```
hci0:   Type: Primary  Bus: SDIO
        BD Address: 88:DA:1A:B3:B4:AF
        UP RUNNING
        ...
```

### Initializing the Bluetooth Stack

If `hciconfig -a` returns no output or the interface is `DOWN`, initialize the Bluetooth stack manually:

```bash
ifconfig rpine0 up
/usr/bin/onebox_util rpine0 enable_protocol 3
/usr/sbin/onebox-protocol-start 4
hciconfig hci0 up
systemctl start bluetooth
```

Verify the interface is now up:

```bash
hciconfig -a
bluetoothctl show
```

The `bluetoothctl show` output should display the controller as `Powered: yes`.

:::tip
These steps need to be run each time the device boots. To automate this, add the commands above to the startup script - please see [Editing the Startup Script](/starting-user-application/editing-startup-script)
:::

:::note
`Failed to open RFKILL control device` may appear in the logs — this is a known harmless warning on this kernel and does not affect Bluetooth functionality.
:::

### Changing the Bluetooth Device Name

By default, the display advertises itself as `imx6qmrsdisp`. To change the Bluetooth device name to something more recognizable:

```bash
hciconfig hci0 name "MConn Display"
hciconfig hci0 leadv
```

The new name will immediately be visible to nearby devices scanning for Bluetooth.

:::tip
This change is not persistent across reboots — the name, discoverability, and pairing settings all reset after a reboot. 

To make them persistent, add the commands above to the startup script - please see [Editing the Startup Script](/starting-user-application/editing-startup-script)
:::

### Basic `bluetoothctl` Commands

The `bluetoothctl` interactive shell is the primary tool for managing Bluetooth on the MConn Display. Launch it with:

```bash
bluetoothctl
```

Once inside the `bluetoothctl` prompt, the following commands are available:

| Command | Description |
|---|---|
| `show` | Show the Bluetooth controller status |
| `discoverable on` | Make the display visible to other devices |
| `discoverable off` | Hide the display from scans |
| `pairable on` | Allow incoming pairing requests |
| `scan on` | Scan for nearby Bluetooth devices |
| `scan off` | Stop scanning |
| `devices` | List discovered devices |
| `pair <MAC>` | Pair with a specific device |
| `connect <MAC>` | Connect to a paired device |
| `disconnect <MAC>` | Disconnect a device |
| `remove <MAC>` | Remove a paired device |
| `quit` | Exit bluetoothctl |

### Pairing a Phone or PC

The following steps walk through pairing a phone or PC with the MConn Display.

**Step 1 — Make the display discoverable**

```bash
bluetoothctl discoverable on
bluetoothctl pairable on
```

:::tip
These settings reset on every reboot. To make them persistent, add the commands above to the startup script - please see [Editing the Startup Script](/starting-user-application/editing-startup-script)
:::

**Step 2 — Launch bluetoothctl and prepare the display**

```bash
bluetoothctl
```

Then inside the prompt:

```
agent on
default-agent
scan on
```

**Step 3 — Initiate pairing from your phone or PC**

On your phone or PC, scan for Bluetooth devices and select `imx6qmrsdisp` (or the custom name you set with `hciconfig`).

**Step 4 — Confirm the passkey**

The display will prompt with a 6-digit passkey:

```
[agent] Confirm passkey 396482 (yes/no):
```

Verify that the passkey matches what is shown on your phone, then type `yes` in the SSH terminal and confirm on your phone.

**Step 5 — Authorize services if prompted**

The display may ask to authorize a Bluetooth profile:

```
[agent] Authorize service 0000111e-0000-1000-8000-00805f9b34fb (yes/no):
```

Type `yes` to allow. Once pairing is complete, the prompt will change to show the paired device name:

```
[John Doe's Phone]#
```

**Step 6 — Exit bluetoothctl**

```
quit
```

or press `Ctrl+D`.

### Advanced Configuration

The MConn Display runs **BlueZ 5.50**, which supports a wide range of advanced Bluetooth features including GATT server/client setup, custom BLE advertising, Bluetooth audio profiles, and service authorization policies.

For advanced configuration, please refer to the official [BlueZ documentation](https://github.com/bluez/bluez/tree/master/doc).

Some examples of advanced configurations available through BlueZ:

- **Custom GATT server** — expose custom BLE services and characteristics to connected devices using `btgatt-server`, useful for building BLE-based data interfaces between the display and other devices
- **BLE advertising data** — customize advertisement packets (device name, manufacturer data, service UUIDs) using `hcitool` or `btmgmt`
- **Bluetooth audio** — the display includes `bluealsa` and `bluealsa-aplay` for routing audio over Bluetooth to connected speakers or headsets
- **Packet capture and debugging** — use `btmon` to capture and inspect Bluetooth traffic in real time, or `btsnoop` to record logs for offline analysis
---
